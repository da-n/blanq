$(document).ready(function() {

  // hack to add classes to core templates tags such as tables so they use bootstrap styles
  // see issue https://github.com/danieldavidson/blanq/issues/1
  $("table").addClass("table");
  
});